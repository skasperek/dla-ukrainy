import { UserSchema, UserDocument, User } from "./users/user.schema";
import {EventSchema, EventDocument, Event} from "./app/event.schema";

export {
    UserSchema, UserDocument, User,
    EventSchema, EventDocument, Event
}